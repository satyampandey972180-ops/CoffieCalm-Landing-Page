# Netlify/Vercel Redirects Configuration
# This file handles 301 redirects for moved or renamed pages

# Redirect old paths to new paths (examples)
# /old-path /new-path 301

# Redirect non-www to www
https://coffiecalm.com/* https://www.coffiecalm.com/:splat 301!

# Redirect HTTP to HTTPS (handled by hosting provider usually)
# http://coffiecalm.com/* https://www.coffiecalm.com/:splat 301!

# SPA fallback - serve index.html for all routes
/* /index.html 200
