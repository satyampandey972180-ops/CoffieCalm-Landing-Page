
  # CoffieCalm Landing Page

  This is the landing page for CoffieCalm - a mindfulness and coffee experience application.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  